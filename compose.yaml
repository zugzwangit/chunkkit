services:
  chunkkit:
    build: .
    environment:
      CHUNKKIT_API_KEYS: '{"demo-token":{"tenant_id":"demo","roles":["platform_admin","runner","viewer"]}}'
    ports:
      - "8000:8000"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
